# recovery quantities vs truncation radius
#   generated by tabulate_recovery.py
#   frac is normalised to 1 at r = R_conv (full footprint)
#   peak_over_rim is the model counterpart of an observed peak/noise
#   reliable = 1 where frac >= 0.20
#   absorp = 1 where the central pixel is negative (absorption
#            against a warmer background; expected at 70/100 um for
#            cold cores, and a flag for the depression models).
#            Such rows carry no frac: the profile is not a
#            declining positive source and truncation is undefined.
#    i   j   k  band      SD_emb      r_as   r_Rconv        peak       I_rim    peak_rim      frac  fwhm_rec     cfoot   cpeakmean  rel absorp
