Here is a summary for the next session.
 
Session Summary: RT Mass Correction — Injection Pipeline & Tier-2 Validation
Current state of the project
Paper: rt_mass_correction.tex in project files. Sections on band selection, injection test, and completeness are already drafted.
Grid: 293 models (i=1–6 × j=1–7 × k=1–8), SD_emb axis = 3e21, 6e21, 1.2e22, 2.4e22 (broken — needs recompute), 4.8e22, 9.6e22 cm⁻². Corrector excludes 2.4e22 block via ok = grid['SD_emb'] != 2.4e22.
Corrector: MassCorrector(cat, sed_mass='M_SED4bs') in fit_mass.py. 2-axis interpolation on (log M_SED4bs, log SD_emb).
 
Injection pipeline (all scripts in outputs)
run_inject.py — main runnable script. Reads config from cwd, falls back to SCRIPTS/Python/. Features:
 
Auto-fills sd_emb and r_be_arcsec from bes_model_params_catalog via tag parsing (no manual entry needed)
Sorts models by R_BE descending, then SD_emb descending (greedy placement)
Physical separation criterion: sqrt((2×R_BE_max)² + 36.3²) arcsec, computed after grid lookup
SCALE_FACTOR support: multiplies all band images and sigma map by factor (for testing higher-SD_emb models in same field); appends _xN to SET_TAG automatically
Writes: inj_SETTAG.Xum.fits, inj_SETTAG.surfdens.fits, src_SETTAG.*, *.omask.fits, inj_SETTAG_truth.txt
Truth table columns: id  model  x_pix  y_pix  local_Sigma  SD_emb  T_BE  rho_BE  M_BE  R_BE_as  scale
local_Sigma = mean over 2×R_BE disk from pre-injection sigma map
Background under stamp: getsf power-9 interpolation (getsf_bg.py)
 
+inject_config_all24.py — 23-model Tier-2 config. Key fields:
pythonRANDOM_SEED = 2026          # change only this for new realizations
SET_TAG     = f'all24_s{RANDOM_SEED}'   # auto
N_CORES     = 2
SCALE_FACTOR = 1.0          # set e.g. 8.0 for 2.4e22 testing
BES_CATALOG = '/path/to/bes_model_params_catalog'
SIGMA_PATH  = '.../inj_surfdens.fits'   # IMPORTANT: correct version
FOOTS_PATH  = '.../sm.Aquila.s.161.obs.foots_injected_hiresSD.fits'
Each model dict needs only tag, stamp_files, surfdens_stamp (optional n_cores).
inject_config_field.py — shared field paths. Now superseded by merging everything into the per-run config (learned lesson: split configs caused path confusion).
inject.py — placement functions. Key: valid_placement_mask uses one-sided Sigma window [SD_emb, SD_emb×√2) (lower bound enforced so corrector always has grid coverage).
getsf_bg.py — getsf background interpolation for stamp flattening.
analyze_recovery.py — standalone analysis script:
bashpython -B analyze_recovery.py truth_table.txt getsf_catalog.cat
 
Reads concatenated catalog using #-offset header (hdr.index(name)-1)
Coordinates: XCO_P = pixel x, YCO_P = pixel y (verified against truth)
Match radius: 1.5 px (= 1/3 of 13.5″ hires beam)
No contamination filter — pure positional matching
Outputs: recovery_SETTAG.txt and recovery_SETTAG.png (3 panels)
 
 
Results from all24_s1111 run
 
27 injected (new truth table format), 8 detected
Median M_SED/M_BE = 0.636, M_corr/M_BE = 0.707
Non-detections: large extended models (k6–k8, IC/SD < 1) and smallest models (k1)
Limitation: using non-source-subtracted Aquila field → but getsf background subtraction handles this; low recovery was actually a column-reading bug (now fixed)
 
Field capacity:
 
SD_emb = 3e21: ~44k valid px → fine for N_CORES=2
SD_emb = 6e21: ~87k valid px → fine
SD_emb = 1.2e22: ~8k valid px → tight, N_CORES=2 for most
SD_emb = 4.8e22: ~0 valid px after edge erosion → nothing fits
SD_emb = 2.4e22: same problem; also injection field too crowded
 
Planned replacement models (undetectable → detectable neighbors at same SD_emb):
 
i1j2k8 → i1j3k5, i2j2k8 → i2j3k5, i2j2k6 → i2j3k3
i3j2k7 → i3j4k1, i3j2k5 → i3j4k1
i5 models: skip (no valid positions regardless)
 
 
Real Aquila catalog correction
Catalog: Aquila_s_sources_ok_cat_Aquila_s_sources_ok_add_cat_thin_Aquila_s_sources_ok_00.cat (141 sources, 3 sub-catalogs merged). Column reading: hdr.index(name)-1 due to # prefix in header.
Key columns: XCO_P=x, YCO_P=y, PEAK^BGF03=Sigma_bg, DUST^TEMP=T_SED, TOTL^MASS=M_SED, AFWHM04=FWHM250.
Results (latest run): 124/141 in hull, median M_corr/M_SED = 1.026, range 0.778–1.317.
Band selection criterion (M_SED4/M_SED3 > 1.3): applies only to the model grid to select the correct correction axis. For real catalogs, getsf's own per-band S/N handling already excludes low-S/N bands — TOTL^MASS reflects the appropriate band combination automatically. No separate 3-band run needed.
 
Next steps
 
Fix 2.4e22 grid block (recompute broken models) and update corrector
Fresh injection run with replacement models (higher-j at i=1,2,3) using source-subtracted field
Scaled field runs (SCALE_FACTOR=8, =16) to test 2.4e22 and 4.8e22 regimes
Multi-seed statistics — run with different RANDOM_SEED values, pool truth tables
Paper: state that Tier-2 injection covers SD_emb = 3e21–1.2e22; 2.4e22 and 4.8e22 validated by Tier-1 hold-out only (correction factor small there: f ≈ 1.02–1.06)
"Limitation: using non-source-subtracted Aquila field → but getsf background subtraction handles this; low recovery was actually a column-reading bug (now fixed)" -- this is incorrect: I'm using source-subtracted Aquila as bg; "2.4e22 (broken — needs recompute)" -- incorrect: it's not broken, because we re-decided to "Band selection criterion (M_SED4/M_SED3 > 1.3): applies only to the model grid to select the correct correction axis."; "i5 models: skip (no valid positions regardless)" -- incorrect: with the scaling factor we can now produce denser Aquila background and place all modelsGood corrections. To summarize the three fixes:
 
Source-subtracted field: You are using the source-subtracted (cb) Aquila images as the injection base — real sources are already removed from the background.
2.4e22 block: Not broken in the sense of needing recomputation. The earlier concern was that the 4-band SED fit collapses at 2.4e22 due to the 160 µm background crossing zero — but we concluded the band-selection criterion (M_SED4/M_SED3 > 1.3) applies only to the grid to choose which correction axis to use, and getsf handles real/injected sources via its own per-band S/N. The 2.4e22 models are valid and the corrector should include them once you decide to remove the exclusion (ok = grid['SD_emb'] != 2.4e22).
i5 and higher-SD_emb models: The SCALE_FACTOR feature now allows placing these by scaling the Aquila field to the appropriate background level, so they are no longer skipped — just require separate scaled-field injection runs.
we should plan to remove the exclusion (ok = grid['SD_emb'] != 2.4e22).Agreed. The exclusion was a temporary workaround when the 4-band collapse at 2.4e22 was thought to corrupt the correction surface. Since that concern is resolved (the band-selection criterion is a grid-level diagnostic, not a catalog-level filter), the exclusion should be removed.
The change is one line in fit_mass.py:
python# remove this line:
ok = grid['SD_emb'] != 2.4e22
This will expand the correction surface to cover all 6 Σ bins including 2.4e22. The corrector will then work for the ~22 real Aquila sources that currently fall in the 2.4e22 region and get OUT_OF_HULL. Worth doing before the next round of catalog corrections and injection tests.
